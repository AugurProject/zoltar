import type { HeroSectionProps } from '../types/components.js'

export function HeroSection(_: HeroSectionProps) {
	return (
		<section className="hero">
			<div className="hero-copy">
				<h1>Augur PLACEHOLDER</h1>
				<p className="lede">A focused onchain workspace for deployment, markets, vaults, reporting, and fork auctions.</p>
			</div>
		</section>
	)
}
