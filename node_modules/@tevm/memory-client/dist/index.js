import { createTransport, createClient, publicActions, walletActions, testActions } from 'viem';
export { createClient, publicActions, testActions, walletActions } from 'viem';
import { createCommon, tevmDefault } from '@tevm/common';
import { requestEip1193, tevmSend, tevmActions } from '@tevm/decorators';
import { createTevmNode } from '@tevm/node';
import { callHandler, contractHandler, dealHandler, deployHandler, dumpStateHandler, getAccountHandler, loadStateHandler, mineHandler, setAccountHandler } from '@tevm/actions';

// src/createClient.js
var createTevmTransport = (options = {}) => {
  const tevmMap = /* @__PURE__ */ new Map();
  return ({ timeout = 2e4, retryCount = 3, chain }) => {
    const dynamicChain = chain && "ethjsCommon" in chain ? (
      /** @type {import('@tevm/common').Common} */
      chain
    ) : chain !== void 0 ? (
      // if user passed in chain instead of common create a common from it with cancun and default eips
      createCommon({ ...chain, hardfork: "prague", loggingLevel: "warn" })
    ) : void 0;
    const common = options.common ?? dynamicChain;
    const id = options.fork?.chainId ?? common?.id ?? -1;
    const tevm = tevmMap.get(id) ?? createTevmNode({ ...options, ...common !== void 0 ? { common } : {} }).extend(requestEip1193()).extend(tevmSend());
    tevmMap.set(id, tevm);
    return (
      /** @type {any} */
      createTransport(
        {
          request: (
            /** @type any */
            tevm.request
          ),
          type: "tevm",
          name: (
            /** options?.name ?? */
            "Tevm transport"
          ),
          key: (
            /* options?.key ?? */
            "tevm"
          ),
          timeout,
          retryCount,
          retryDelay: (
            /* options?.retryDelay ?? */
            150
          )
        },
        { tevm }
      )
    );
  };
};
var tevmViemActions = () => {
  const extension = (client) => {
    const { call, contract, deploy, mine, loadState, dumpState, setAccount, getAccount, ready, deal } = client.transport.tevm.extend(tevmActions());
    return {
      tevmReady: ready,
      tevmCall: call,
      tevmContract: contract,
      tevmDeploy: deploy,
      tevmMine: mine,
      tevmLoadState: loadState,
      tevmDumpState: dumpState,
      tevmSetAccount: setAccount,
      tevmGetAccount: getAccount,
      tevmDeal: deal
    };
  };
  return extension;
};

// src/createMemoryClient.js
var createMemoryClient = (
  /** @type {import('./CreateMemoryClientFn.js').CreateMemoryClientFn} */
  /** @param {any} options */
  ((options) => {
    const miningConfig = (() => {
      if (options?.miningConfig !== void 0) {
        return options.miningConfig;
      }
      const mining = options?.mining;
      if (mining === void 0) {
        return void 0;
      }
      if (typeof mining.interval === "number") {
        return { type: "interval", blockTime: mining.interval / 1e3 };
      }
      if (typeof mining.auto === "boolean") {
        return { type: mining.auto ? "auto" : "manual" };
      }
      return void 0;
    })();
    const normalizedOptions = {
      ...options,
      ...miningConfig !== void 0 ? { miningConfig } : {}
    };
    const common = (() => {
      if (normalizedOptions?.common !== void 0) {
        return normalizedOptions.common;
      }
      if (normalizedOptions?.fork?.transport) {
        return void 0;
      }
      return tevmDefault;
    })();
    const chain = (() => {
      if (common === void 0) {
        return void 0;
      }
      if (normalizedOptions?.fork?.chainId === void 0) {
        return common;
      }
      return createCommon({
        ...common,
        id: normalizedOptions.fork.chainId,
        loggingLevel: normalizedOptions.loggingLevel ?? "warn",
        hardfork: common.ethjsCommon.hardfork() ?? "prague",
        eips: common.ethjsCommon.eips(),
        customCrypto: common.ethjsCommon.customCrypto
      });
    })();
    const memoryClient = createClient({
      ...normalizedOptions,
      cacheTime: normalizedOptions.cacheTime ?? 0,
      transport: createTevmTransport(
        /** @type {import('@tevm/node').TevmNodeOptions} */
        {
          ...normalizedOptions,
          ...chain !== void 0 ? { common: chain } : {}
        }
      ),
      type: "tevm",
      ...chain !== void 0 ? { chain } : {}
    }).extend(
      /** @type {(client: import('viem').Client<import('viem').Transport, undefined, undefined>) => import('./TevmViemActionsApi.js').TevmViemActionsApi} */
      tevmViemActions()
    ).extend(publicActions).extend(walletActions).extend(testActions({ mode: "anvil" }));
    return (
      /** @type {any} */
      memoryClient
    );
  })
);
var tevmCall = async (client, params) => {
  return callHandler(client.transport.tevm)(params);
};
var tevmContract = async (client, params) => {
  return contractHandler(client.transport.tevm)(
    /** @type {any}*/
    params
  );
};
var tevmDeal = (node) => {
  return dealHandler(node);
};
var tevmDeploy = async (client, params) => {
  return deployHandler(client.transport.tevm)(params);
};
var tevmDumpState = async (client) => {
  return dumpStateHandler(client.transport.tevm)();
};
var tevmGetAccount = async (client, params) => {
  return getAccountHandler(client.transport.tevm)(params);
};
var tevmLoadState = async (client, params) => {
  return loadStateHandler(client.transport.tevm)(params);
};
var tevmMine = async (client, params) => {
  return mineHandler(client.transport.tevm)(params);
};

// src/tevmReady.js
var tevmReady = async (client) => {
  return client.transport.tevm.ready();
};
var tevmSetAccount = async (client, params) => {
  return setAccountHandler(client.transport.tevm)(params);
};

export { createMemoryClient, createTevmTransport, tevmCall, tevmContract, tevmDeal, tevmDeploy, tevmDumpState, tevmGetAccount, tevmLoadState, tevmMine, tevmReady, tevmSetAccount, tevmViemActions };
//# sourceMappingURL=index.js.map
//# sourceMappingURL=index.js.map