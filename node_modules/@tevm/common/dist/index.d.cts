import { Common as Common$1, CustomCrypto } from '@evmts/zevm/common';
export { AccountFields, CliqueConfig, ConsensusAlgorithm, ConsensusType, CustomCrypto, StateManagerInterface as EvmStateManagerInterface, StorageDump, StorageRange } from '@evmts/zevm/common';
import { Prettify, Chain } from 'viem/chains';
import { LogOptions } from '@tevm/logger';

/**
 * Common is the main representation of chain specific configuration for tevm clients.
 *
 * Tevm specific chain configuration wrapping a viem chain and ZEVM common.
 * Common contains the common configuration set between all chains such as fee information, hardfork information, eip information, predeployed contracts, default block explorers and more.
 * Extends the Viem Chain type with the ZEVM-backed Common instance used by the VM.
 * @example
 * ```typescript
 * import { optimism, Common } from 'tevm/common'
 * import { createMemoryClient } from 'tevm'}
 *
 * const createClient = (common: Common) => {
 *   const client = createMemoryClient({
 *     common,
 *   })
 *   return client
 * }
 *
 * const client = createClient(optimism)
 * ```
 * @see [createCommon](https://tevm.sh/reference/tevm/common/functions/createcommon/)
 * @see [Tevm client docs](https://tevm.sh/learn/clients/)
 */
type Common = Prettify<Chain & {
    ethjsCommon: Common$1;
    copy: () => Common;
}>;

/**
 * Ethereum hardfork options. Default option is currently prague.
 * If you use older hardforks you might run into issues with EIPs not being supported.
 * @example
 * ```typescript
 * import { createCommon, mainnet } from 'tevm/common'`
 *
 * const hardfork: Hardfork = 'shanghai'
 *
 * const common = createCommon({
 *   ...mainnet,
 *   hardfork,
 * })
 * ```
 * @see [createCommon](https://tevm.sh/reference/tevm/common/functions/createcommon/)
 */
type Hardfork = 'chainstart' | 'homestead' | 'dao' | 'tangerineWhistle' | 'spuriousDragon' | 'byzantium' | 'constantinople' | 'petersburg' | 'istanbul' | 'muirGlacier' | 'berlin' | 'london' | 'arrowGlacier' | 'grayGlacier' | 'mergeNetsplitBlock' | 'mergeForkIdTransition' | 'paris' | 'shanghai' | 'cancun' | 'prague' | 'osaka';

/**
 * @property {Hardfork} [hardfork='prague'] - Hardfork to use
 * @property {ReadonlyArray<number>} [eips=[]] - EIPs to enable in addition to hardfork-native EIPs
 * @property {LogOptions['level']} loggingLevel - Tevm logger instance
 * @property {CustomCrypto} [customCrypto] - Custom crypto implementations
 * Options for creating a Tevm Common instance
 * @example
 * ```typescript
 * import { mainnet, createCommon, type CommonOptions } from 'tevm/common'
 *
 * const opts: CommonOptions = {
 *   ...mainnet,
 *   hardfork: 'london',
 * }
 *
 * const common = createCommon(opts)
 * ```
 *
 * You can also create a Common instance from viem chains:
 * @example
 * ```typescript
 * import { mainnet } from 'viem/chains'
 * import { createCommon } from 'tevm/common'
 *
 * const common = createCommon({
 *   ...mainnet,
 *   hardfork: 'prague',
 * })
 * ```
 *
 * @see [createCommon](https://tevm.sh/reference/tevm/common/functions/createcommon/)
 */
type CommonOptions = Chain & {
    /**
     * Hardfork to use. Defaults to `prague`
     * @default 'prague'
     */
    hardfork?: Hardfork | undefined;
    /**
     * EIPs to enable in addition to hardfork-native EIPs. Defaults to `[]`.
     * @default []
     */
    eips?: ReadonlyArray<number> | undefined;
    /**
     * Logging level of the Tevm logger instance
     * @default 'warn'
     */
    loggingLevel?: LogOptions['level'] | undefined;
    /**
     * Custom crypto implementations
     * For EIP-4844 support kzg must be passed
     * @warning KZG can add a significant amount of bundle size to an app
     * A mock KZG implementation is available via createMockKzg but must be passed explicitly
     * @example
     * ```typescript
     * import  { createMemoryClient } from 'tevm'
     * import  { mainnet } from 'tevm/common'
     * import { createMockKzg } from 'tevm/crypto'
     *
     * const common = createCommon({
     *   ...mainnet,
     *   customCrypto: {
     *     kzg: createMockKzg(),
     *     ...customCrypto,
     *   },
     * })
     * ```
     */
    customCrypto?: CustomCrypto;
};

declare function createCommon({ customCrypto, loggingLevel, hardfork, eips, ...chain }: CommonOptions): Common;

/**
 * The interface of the custom crypto for KZG implemented by `createMockKzg`.
 * The real KZG commitment implementation can add significant bundle size,
 * so this is a useful explicit opt-in alternative for smaller bundles.
 * @example
 * ```typescript
 * import { createCommon, createMockKzg, mainnet, type MockKzg } from 'tevm/common'
 *
 * const kzg: MockKzg = createMockKzg()
 *
 * const common = createCommon({
 *   ...mainnet,
 *   customCrypto: {
 *     kzg,
 *   },
 * })
 * ```
 * @see [createMockKzg](https://tevm.sh/reference/tevm/common/functions/createmockkzg/)
 * @see [createCommon](https://tevm.sh/reference/tevm/common/functions/createcommon/)
 */
type MockKzg = {
    loadTrustedSetup: (trustedSetup?: any) => number;
    freeTrustedSetup: () => void;
    blobToKzgCommitment: (blob: string) => string;
    computeBlobKzgProof: (blob: string, commitment: string) => string;
    verifyBlobKzgProofBatch: (blobs: string[], commitments: string[], proofs: string[]) => boolean;
    verifyKzgProof: (commitment: string, z: string, y: string, proof: string) => boolean;
    verifyBlobKzgProof: (blob: string, commitment: string, proof: string) => boolean;
    computeBlobProof: (blob: string, commitment: string) => string;
    verifyProof: (commitment: string, z: string, y: string, proof: string) => boolean;
    verifyBlobProofBatch: (blobs: string[], commitments: string[], proofs: string[]) => boolean;
    computeCells: (blob: string) => string[];
    computeCellsAndProofs: (blob: string) => [string[], string[]];
    recoverCellsAndProofs: (indices: number[], cells: string[]) => [string[], string[]];
    verifyCellKzgProofBatch: (commitments: string[], indices: number[], cells: string[], proofs: string[]) => boolean;
};

declare function createMockKzg(): MockKzg;

/**
 * The default chain if no fork url is passed
 * @type {import('../Common.js').Common}
 */
declare const tevmDefault: Common;

/**
 * `Common` for Abstract Testnet (chain ID 11124). Explorer: https://sepolia.abscan.org. Default RPC: https://api.testnet.abs.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const abstractTestnet: Common;

/**
 * `Common` for Acala (chain ID 787). Explorer: https://blockscout.acala.network. Default RPC: https://eth-rpc-acala.aca-api.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const acala: Common;

/**
 * `Common` for Ancient8 (chain ID 888888888). Explorer: https://scan.ancient8.gg. Default RPC: https://rpc.ancient8.gg.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const ancient8: Common;

/**
 * `Common` for Ancient8 Testnet (chain ID 28122024). Explorer: https://scanv2-testnet.ancient8.gg. Default RPC: https://rpcv2-testnet.ancient8.gg.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const ancient8Sepolia: Common;

/**
 * `Common` for Anvil (chain ID 31337). Explorer: —. Default RPC: http://127.0.0.1:8545.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const anvil: Common;

/**
 * `Common` for APEX Testnet (chain ID 3993). Explorer: https://exp-testnet.apexlayer.xyz. Default RPC: https://rpc-testnet.apexlayer.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const apexTestnet: Common;

/**
 * `Common` for Arbitrum One (chain ID 42161). Explorer: https://arbiscan.io. Default RPC: https://arb1.arbitrum.io/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const arbitrum: Common;

/**
 * `Common` for Arbitrum Goerli (chain ID 421613). Explorer: https://goerli.arbiscan.io. Default RPC: https://goerli-rollup.arbitrum.io/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const arbitrumGoerli: Common;

/**
 * `Common` for Arbitrum Nova (chain ID 42170). Explorer: https://nova.arbiscan.io. Default RPC: https://nova.arbitrum.io/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const arbitrumNova: Common;

/**
 * `Common` for AssetChain Testnet (chain ID 42421). Explorer: https://scan-testnet.assetchain.org. Default RPC: https://enugu-rpc.assetchain.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const assetChainTestnet: Common;

/**
 * `Common` for Astar (chain ID 592). Explorer: https://astar.subscan.io. Default RPC: https://astar.api.onfinality.io/public.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const astar: Common;

/**
 * `Common` for Astar zkEVM (chain ID 3776). Explorer: https://astar-zkevm.explorer.startale.com. Default RPC: https://rpc-zkevm.astar.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const astarZkEVM: Common;

/**
 * `Common` for Astar zkEVM Testnet zKyoto (chain ID 6038361). Explorer: https://zkyoto.explorer.startale.com. Default RPC: https://rpc.startale.com/zkyoto.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const astarZkyoto: Common;

/**
 * `Common` for Arbitrum Sepolia (chain ID 421614). Explorer: https://sepolia.arbiscan.io. Default RPC: https://sepolia-rollup.arbitrum.io/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const arbitrumSepolia: Common;

/**
 * `Common` for Areon Network (chain ID 463). Explorer: https://areonscan.com. Default RPC: https://mainnet-rpc.areon.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const areonNetwork: Common;

/**
 * `Common` for Areon Network Testnet (chain ID 462). Explorer: https://areonscan.com. Default RPC: https://testnet-rpc.areon.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const areonNetworkTestnet: Common;

/**
 * `Common` for Artela Testnet (chain ID 11822). Explorer: https://betanet-scan.artela.network. Default RPC: https://betanet-rpc1.artela.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const artelaTestnet: Common;

/**
 * `Common` for Atleta Olympia (chain ID 2340). Explorer: https://blockscout.atleta.network. Default RPC: https://testnet-rpc.atleta.network:9944.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const atletaOlympia: Common;

/**
 * `Common` for Aurora (chain ID 1313161554). Explorer: https://aurorascan.dev. Default RPC: https://mainnet.aurora.dev.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const aurora: Common;

/**
 * `Common` for Aurora Testnet (chain ID 1313161555). Explorer: https://testnet.aurorascan.dev. Default RPC: https://testnet.aurora.dev.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const auroraTestnet: Common;

/**
 * `Common` for Auroria Testnet (chain ID 205205). Explorer: https://auroria.explorer.stratisevm.com. Default RPC: https://auroria.rpc.stratisevm.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const auroria: Common;

/**
 * `Common` for Avalanche (chain ID 43114). Explorer: https://snowtrace.io. Default RPC: https://api.avax.network/ext/bc/C/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const avalanche: Common;

/**
 * `Common` for Avalanche Fuji (chain ID 43113). Explorer: https://testnet.snowtrace.io. Default RPC: https://api.avax-test.network/ext/bc/C/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const avalancheFuji: Common;

/**
 * `Common` for B3 Sepolia (chain ID 1993). Explorer: https://sepolia.explorer.b3.fun. Default RPC: https://sepolia.b3.fun/http.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const b3Sepolia: Common;

/**
 * `Common` for B3 (chain ID 8333). Explorer: https://explorer.b3.fun. Default RPC: https://mainnet-rpc.b3.fun/http.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const b3: Common;

/**
 * `Common` for Bahamut (chain ID 5165). Explorer: https://www.ftnscan.com. Default RPC: https://rpc1.bahamut.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bahamut: Common;

/**
 * `Common` for Base (chain ID 8453). Explorer: https://basescan.org. Default RPC: https://mainnet.base.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const base: Common;

/**
 * `Common` for Base Goerli (chain ID 84531). Explorer: https://goerli.basescan.org. Default RPC: https://goerli.base.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const baseGoerli: Common;

/**
 * `Common` for Base Sepolia (chain ID 84532). Explorer: https://sepolia.basescan.org. Default RPC: https://sepolia.base.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const baseSepolia: Common;

/**
 * `Common` for Beam (chain ID 4337). Explorer: https://subnets.avax.network/beam. Default RPC: https://build.onbeam.com/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const beam: Common;

/**
 * `Common` for Beam Testnet (chain ID 13337). Explorer: https://subnets-test.avax.network/beam. Default RPC: https://build.onbeam.com/rpc/testnet.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const beamTestnet: Common;

/**
 * `Common` for Bear Network Chain Mainnet (chain ID 641230). Explorer: https://brnkscan.bearnetwork.net. Default RPC: https://brnkc-mainnet.bearnetwork.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bearNetworkChainMainnet: Common;

/**
 * `Common` for Bear Network Chain Testnet (chain ID 751230). Explorer: https://brnktest-scan.bearnetwork.net. Default RPC: https://brnkc-test.bearnetwork.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bearNetworkChainTestnet: Common;

/**
 * `Common` for Berachain Artio (chain ID 80085). Explorer: https://artio.beratrail.io. Default RPC: https://artio.rpc.berachain.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const berachainTestnet: Common;

/**
 * `Common` for Berachain bArtio (chain ID 80084). Explorer: https://bartio.beratrail.io. Default RPC: https://bartio.rpc.berachain.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const berachainTestnetbArtio: Common;

/**
 * `Common` for BEVM Mainnet (chain ID 11501). Explorer: https://scan-mainnet.bevm.io. Default RPC: https://rpc-mainnet-1.bevm.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bevmMainnet: Common;

/**
 * `Common` for KUB Mainnet (chain ID 96). Explorer: https://www.bkcscan.com. Default RPC: https://rpc.bitkubchain.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bitkub: Common;

/**
 * `Common` for Bitkub Testnet (chain ID 25925). Explorer: https://testnet.bkcscan.com. Default RPC: https://rpc-testnet.bitkubchain.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bitkubTestnet: Common;

/**
 * `Common` for BitTorrent (chain ID 199). Explorer: https://bttcscan.com. Default RPC: https://rpc.bittorrentchain.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bitTorrent: Common;

/**
 * `Common` for BitTorrent Chain Testnet (chain ID 1028). Explorer: https://testnet.bttcscan.com. Default RPC: https://testrpc.bittorrentchain.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bitTorrentTestnet: Common;

/**
 * `Common` for Blast (chain ID 81457). Explorer: https://blastscan.io. Default RPC: https://rpc.blast.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const blast: Common;

/**
 * `Common` for Blast Sepolia (chain ID 168587773). Explorer: https://sepolia.blastscan.io. Default RPC: https://sepolia.blast.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const blastSepolia: Common;

/**
 * `Common` for BOB (chain ID 60808). Explorer: https://explorer.gobob.xyz. Default RPC: https://rpc.gobob.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bob: Common;

/**
 * `Common` for BOB Sepolia (chain ID 808813). Explorer: https://bob-sepolia.explorer.gobob.xyz. Default RPC: https://bob-sepolia.rpc.gobob.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bobSepolia: Common;

/**
 * `Common` for Boba Network (chain ID 288). Explorer: https://bobascan.com. Default RPC: https://mainnet.boba.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const boba: Common;

/**
 * `Common` for Boba Sepolia (chain ID 28882). Explorer: https://testnet.bobascan.com. Default RPC: https://sepolia.boba.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bobaSepolia: Common;

/**
 * `Common` for Botanix Testnet (chain ID 3636). Explorer: https://testnet.botanixscan.io. Default RPC: https://node.botanixlabs.dev.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const botanixTestnet: Common;

/**
 * `Common` for Bronos (chain ID 1039). Explorer: https://broscan.bronos.org. Default RPC: https://evm.bronos.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bronos: Common;

/**
 * `Common` for Bronos Testnet (chain ID 1038). Explorer: https://tbroscan.bronos.org. Default RPC: https://evm-testnet.bronos.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bronosTestnet: Common;

/**
 * `Common` for BNB Smart Chain (chain ID 56). Explorer: https://bscscan.com. Default RPC: https://56.rpc.thirdweb.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bsc: Common;

/**
 * `Common` for BNB Smart Chain Testnet (chain ID 97). Explorer: https://testnet.bscscan.com. Default RPC: https://data-seed-prebsc-1-s1.bnbchain.org:8545.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bscTestnet: Common;

/**
 * `Common` for BNB Greenfield Chain (chain ID 1017). Explorer: https://greenfieldscan.com. Default RPC: https://greenfield-chain.bnbchain.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bscGreenfield: Common;

/**
 * `Common` for Bitlayer (chain ID 200901). Explorer: https://www.btrscan.com. Default RPC: https://rpc.bitlayer.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const btr: Common;

/**
 * `Common` for Bitlayer Testnet (chain ID 200810). Explorer: https://testnet.btrscan.com. Default RPC: https://testnet-rpc.bitlayer.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const btrTestnet: Common;

/**
 * `Common` for BlackFort Exchange Network (chain ID 4999). Explorer: https://explorer.blackfort.network. Default RPC: https://mainnet.blackfort.network/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bxn: Common;

/**
 * `Common` for BlackFort Exchange Network Testnet (chain ID 4777). Explorer: https://testnet-explorer.blackfort.network. Default RPC: https://testnet.blackfort.network/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const bxnTestnet: Common;

/**
 * `Common` for Canto (chain ID 7700). Explorer: https://tuber.build. Default RPC: https://canto.gravitychain.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const canto: Common;

/**
 * `Common` for Celo (chain ID 42220). Explorer: https://celoscan.io. Default RPC: https://forno.celo.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const celo: Common;

/**
 * `Common` for Alfajores (chain ID 44787). Explorer: https://celo-alfajores.blockscout.com. Default RPC: https://alfajores-forno.celo-testnet.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const celoAlfajores: Common;

/**
 * `Common` for Chiliz Chain (chain ID 88888). Explorer: https://scan.chiliz.com. Default RPC: https://rpc.chiliz.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const chiliz: Common;

/**
 * `Common` for Chips Network (chain ID 2882). Explorer: —. Default RPC: https://node.chips.ooo/wasp/api/v1/chains/iota1pp3d3mnap3ufmgqnjsnw344sqmf5svjh26y2khnmc89sv6788y3r207a8fn/evm.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const chips: Common;

/**
 * `Common` for Ethereum Classic (chain ID 61). Explorer: https://blockscout.com/etc/mainnet. Default RPC: https://etc.rivet.link.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const classic: Common;

/**
 * `Common` for Conflux eSpace (chain ID 1030). Explorer: https://evm.confluxscan.org. Default RPC: https://evm.confluxrpc.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const confluxESpace: Common;

/**
 * `Common` for Conflux eSpace Testnet (chain ID 71). Explorer: https://evmtestnet.confluxscan.org. Default RPC: https://evmtestnet.confluxrpc.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const confluxESpaceTestnet: Common;

/**
 * `Common` for Core Dao (chain ID 1116). Explorer: https://scan.coredao.org. Default RPC: https://rpc.coredao.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const coreDao: Common;

/**
 * `Common` for Crab Network (chain ID 44). Explorer: https://crab-scan.darwinia.network. Default RPC: https://crab-rpc.darwinia.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const crab: Common;

/**
 * `Common` for Cronos Mainnet (chain ID 25). Explorer: https://explorer.cronos.org. Default RPC: https://evm.cronos.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const cronos: Common;

/**
 * `Common` for Cronos zkEVM Mainnet (chain ID 388). Explorer: https://explorer.zkevm.cronos.org. Default RPC: https://mainnet.zkevm.cronos.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const cronoszkEVM: Common;

/**
 * `Common` for Cronos zkEVM Testnet (chain ID 282). Explorer: https://explorer.zkevm.cronos.org/testnet. Default RPC: https://testnet.zkevm.cronos.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const cronoszkEVMTestnet: Common;

/**
 * `Common` for Cronos Testnet (chain ID 338). Explorer: https://explorer.cronos.org/testnet. Default RPC: https://evm-t3.cronos.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const cronosTestnet: Common;

/**
 * `Common` for Crossbell (chain ID 3737). Explorer: https://scan.crossbell.io. Default RPC: https://rpc.crossbell.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const crossbell: Common;

/**
 * `Common` for Curtis (chain ID 33111). Explorer: https://explorer.curtis.apechain.com. Default RPC: https://rpc.curtis.apechain.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const curtis: Common;

/**
 * `Common` for Cyber (chain ID 7560). Explorer: https://cyberscan.co. Default RPC: https://cyber.alt.technology.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const cyber: Common;

/**
 * `Common` for Cyber Testnet (chain ID 111557560). Explorer: https://testnet.cyberscan.co. Default RPC: https://cyber-testnet.alt.technology.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const cyberTestnet: Common;

/**
 * `Common` for Darwinia Network (chain ID 46). Explorer: https://explorer.darwinia.network. Default RPC: https://rpc.darwinia.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const darwinia: Common;

/**
 * `Common` for Dchain (chain ID 2716446429837000). Explorer: https://dchain-2716446429837000-1.sagaexplorer.io. Default RPC: https://dchain-2716446429837000-1.jsonrpc.sagarpc.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const dchain: Common;

/**
 * `Common` for Dchain Testnet (chain ID 2713017997578000). Explorer: https://dchaintestnet-2713017997578000-1.testnet.sagaexplorer.io. Default RPC: https://dchaintestnet-2713017997578000-1.jsonrpc.testnet.sagarpc.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const dchainTestnet: Common;

/**
 * `Common` for DeFiChain EVM Mainnet (chain ID 1130). Explorer: https://meta.defiscan.live. Default RPC: https://eth.mainnet.ocean.jellyfishsdk.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const defichainEvm: Common;

/**
 * `Common` for DeFiChain EVM Testnet (chain ID 1131). Explorer: https://meta.defiscan.live/?network=TestNet. Default RPC: https://eth.testnet.ocean.jellyfishsdk.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const defichainEvmTestnet: Common;

/**
 * `Common` for Degen (chain ID 666666666). Explorer: https://explorer.degen.tips. Default RPC: https://rpc.degen.tips.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const degen: Common;

/**
 * `Common` for DFK Chain (chain ID 53935). Explorer: https://subnets.avax.network/defi-kingdoms. Default RPC: https://subnets.avax.network/defi-kingdoms/dfk-chain/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const dfk: Common;

/**
 * `Common` for DODOchain Testnet (chain ID 53457). Explorer: https://testnet-scan.dodochain.com. Default RPC: https://dodochain-testnet.alt.technology.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const dodochainTestnet: Common;

/**
 * `Common` for Dogechain (chain ID 2000). Explorer: https://explorer.dogechain.dog. Default RPC: https://rpc.dogechain.dog.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const dogechain: Common;

/**
 * `Common` for DreyerX Mainnet (chain ID 23451). Explorer: https://scan.dreyerx.com. Default RPC: https://rpc.dreyerx.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const dreyerxMainnet: Common;

/**
 * `Common` for DreyerX Testnet (chain ID 23452). Explorer: https://testnet-scan.dreyerx.com. Default RPC: http://testnet-rpc.dreyerx.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const dreyerxTestnet: Common;

/**
 * `Common` for Edgeless Network (chain ID 2026). Explorer: https://explorer.edgeless.network. Default RPC: https://rpc.edgeless.network/http.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const edgeless: Common;

/**
 * `Common` for Edgeless Testnet (chain ID 202). Explorer: https://testnet.explorer.edgeless.network. Default RPC: https://edgeless-testnet.rpc.caldera.xyz/http.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const edgelessTestnet: Common;

/**
 * `Common` for Edgeware EdgeEVM Mainnet (chain ID 2021). Explorer: https://edgscan.live. Default RPC: https://edgeware-evm.jelliedowl.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const edgeware: Common;

/**
 * `Common` for Beresheet BereEVM Testnet (chain ID 2022). Explorer: https://testnet.edgscan.live. Default RPC: https://beresheet-evm.jelliedowl.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const edgewareTestnet: Common;

/**
 * `Common` for Horizen EON (chain ID 7332). Explorer: https://eon-explorer.horizenlabs.io. Default RPC: https://eon-rpc.horizenlabs.io/ethv1.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const eon: Common;

/**
 * `Common` for EOS EVM (chain ID 17777). Explorer: https://explorer.evm.eosnetwork.com. Default RPC: https://api.evm.eosnetwork.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const eos: Common;

/**
 * `Common` for EOS EVM Testnet (chain ID 15557). Explorer: https://explorer.testnet.evm.eosnetwork.com. Default RPC: https://api.testnet.evm.eosnetwork.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const eosTestnet: Common;

/**
 * `Common` for Etherlink (chain ID 42793). Explorer: https://explorer.etherlink.com. Default RPC: https://node.mainnet.etherlink.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const etherlink: Common;

/**
 * `Common` for Etherlink Testnet (chain ID 128123). Explorer: https://testnet.explorer.etherlink.com. Default RPC: https://node.ghostnet.etherlink.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const etherlinkTestnet: Common;

/**
 * `Common` for Evmos (chain ID 9001). Explorer: https://escan.live. Default RPC: https://eth.bd.evmos.org:8545.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const evmos: Common;

/**
 * `Common` for Evmos Testnet (chain ID 9000). Explorer: https://evm.evmos.dev/. Default RPC: https://eth.bd.evmos.dev:8545.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const evmosTestnet: Common;

/**
 * `Common` for Fantom (chain ID 250). Explorer: https://ftmscan.com. Default RPC: https://250.rpc.thirdweb.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const fantom: Common;

/**
 * `Common` for Fantom Sonic Open Testnet (chain ID 64240). Explorer: https://public-sonic.fantom.network. Default RPC: https://rpcapi.sonic.fantom.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const fantomSonicTestnet: Common;

/**
 * `Common` for Fantom Testnet (chain ID 4002). Explorer: https://testnet.ftmscan.com. Default RPC: https://rpc.testnet.fantom.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const fantomTestnet: Common;

/**
 * `Common` for Fibo Chain (chain ID 12306). Explorer: https://scan.fibochain.org. Default RPC: https://network.hzroc.art.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const fibo: Common;

/**
 * `Common` for Filecoin Mainnet (chain ID 314). Explorer: https://filfox.info/en. Default RPC: https://api.node.glif.io/rpc/v1.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const filecoin: Common;

/**
 * `Common` for Filecoin Calibration (chain ID 314159). Explorer: https://calibration.filscan.io. Default RPC: https://api.calibration.node.glif.io/rpc/v1.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const filecoinCalibration: Common;

/**
 * `Common` for Filecoin Hyperspace (chain ID 3141). Explorer: https://hyperspace.filfox.info/en. Default RPC: https://api.hyperspace.node.glif.io/rpc/v1.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const filecoinHyperspace: Common;

/**
 * `Common` for Flare Mainnet (chain ID 14). Explorer: https://flare-explorer.flare.network. Default RPC: https://flare-api.flare.network/ext/C/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const flare: Common;

/**
 * `Common` for Flare Testnet Coston2 (chain ID 114). Explorer: https://coston2-explorer.flare.network. Default RPC: https://coston2-api.flare.network/ext/C/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const flareTestnet: Common;

/**
 * `Common` for Flow EVM Previewnet (chain ID 646). Explorer: https://previewnet.flowdiver.io. Default RPC: https://previewnet.evm.nodes.onflow.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const flowPreviewnet: Common;

/**
 * `Common` for Flow EVM Mainnet (chain ID 747). Explorer: https://evm.flowscan.io. Default RPC: https://mainnet.evm.nodes.onflow.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const flowMainnet: Common;

/**
 * `Common` for Flow EVM Testnet (chain ID 545). Explorer: https://evm-testnet.flowscan.io. Default RPC: https://testnet.evm.nodes.onflow.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const flowTestnet: Common;

/**
 * `Common` for Fluence (chain ID 9999999). Explorer: https://blockscout.mainnet.fluence.dev. Default RPC: https://rpc.mainnet.fluence.dev.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const fluence: Common;

/**
 * `Common` for Fluence Stage (chain ID 123420000220). Explorer: https://blockscout.stage.fluence.dev. Default RPC: https://rpc.stage.fluence.dev.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const fluenceStage: Common;

/**
 * `Common` for Fluence Testnet (chain ID 52164803). Explorer: https://blockscout.testnet.fluence.dev. Default RPC: https://rpc.testnet.fluence.dev.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const fluenceTestnet: Common;

/**
 * `Common` for Forma (chain ID 984122). Explorer: https://explorer.forma.art. Default RPC: https://rpc.forma.art.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const forma: Common;

/**
 * `Common` for Foundry (chain ID 31337). Explorer: —. Default RPC: http://127.0.0.1:8545.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const foundry: Common;

/**
 * `Common` for Fraxtal (chain ID 252). Explorer: https://fraxscan.com. Default RPC: https://rpc.frax.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const fraxtal: Common;

/**
 * `Common` for Fraxtal Testnet (chain ID 2522). Explorer: https://holesky.fraxscan.com. Default RPC: https://rpc.testnet.frax.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const fraxtalTestnet: Common;

/**
 * `Common` for Funki (chain ID 33979). Explorer: https://funkiscan.io. Default RPC: https://rpc-mainnet.funkichain.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const funkiMainnet: Common;

/**
 * `Common` for Funki Sepolia Sandbox (chain ID 3397901). Explorer: https://sepolia-sandbox.funkichain.com/. Default RPC: https://funki-testnet.alt.technology.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const funkiSepolia: Common;

/**
 * `Common` for Fuse (chain ID 122). Explorer: https://explorer.fuse.io. Default RPC: https://rpc.fuse.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const fuse: Common;

/**
 * `Common` for Fuse Sparknet (chain ID 123). Explorer: https://explorer.fusespark.io. Default RPC: https://rpc.fusespark.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const fuseSparknet: Common;

/**
 * `Common` for IoTeX (chain ID 4689). Explorer: https://iotexscan.io. Default RPC: https://babel-api.mainnet.iotex.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const iotex: Common;

/**
 * `Common` for IoTeX Testnet (chain ID 4690). Explorer: https://testnet.iotexscan.io. Default RPC: https://babel-api.testnet.iotex.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const iotexTestnet: Common;

/**
 * `Common` for JB Chain (chain ID 8899). Explorer: https://exp-l1.jibchain.net. Default RPC: https://rpc-l1.jibchain.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const jbc: Common;

/**
 * `Common` for Jibchain Testnet (chain ID 88991). Explorer: https://exp.testnet.jibchain.net. Default RPC: https://rpc.testnet.jibchain.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const jbcTestnet: Common;

/**
 * `Common` for Karura (chain ID 686). Explorer: https://blockscout.karura.network. Default RPC: https://eth-rpc-karura.aca-api.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const karura: Common;

/**
 * `Common` for Horizen Gobi Testnet (chain ID 1663). Explorer: https://gobi-explorer.horizen.io. Default RPC: https://gobi-testnet.horizenlabs.io/ethv1.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const gobi: Common;

/**
 * `Common` for Goerli (chain ID 5). Explorer: https://goerli.etherscan.io. Default RPC: https://5.rpc.thirdweb.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const goerli: Common;

/**
 * `Common` for Gnosis (chain ID 100). Explorer: https://gnosisscan.io. Default RPC: https://rpc.gnosischain.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const gnosis: Common;

/**
 * `Common` for Gnosis Chiado (chain ID 10200). Explorer: https://blockscout.chiadochain.net. Default RPC: https://rpc.chiadochain.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const gnosisChiado: Common;

/**
 * `Common` for Gravity Alpha Mainnet (chain ID 1625). Explorer: https://explorer.gravity.xyz. Default RPC: https://rpc.gravity.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const gravity: Common;

/**
 * `Common` for Ham (chain ID 5112). Explorer: https://explorer.ham.fun. Default RPC: https://rpc.ham.fun.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const ham: Common;

/**
 * `Common` for Hardhat (chain ID 31337). Explorer: —. Default RPC: http://127.0.0.1:8545.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const hardhat: Common;

/**
 * `Common` for Harmony One (chain ID 1666600000). Explorer: https://explorer.harmony.one. Default RPC: https://1666600000.rpc.thirdweb.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const harmonyOne: Common;

/**
 * `Common` for HashKey Chain Testnet (chain ID 133). Explorer: https://testnet-explorer.hsk.xyz. Default RPC: https://testnet.hsk.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const hashkeyTestnet: Common;

/**
 * `Common` for HAQQ Mainnet (chain ID 11235). Explorer: https://explorer.haqq.network. Default RPC: https://rpc.eth.haqq.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const haqqMainnet: Common;

/**
 * `Common` for HAQQ Testedge 2 (chain ID 54211). Explorer: https://explorer.testedge2.haqq.network. Default RPC: https://rpc.eth.testedge2.haqq.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const haqqTestedge2: Common;

/**
 * `Common` for Hedera Mainnet (chain ID 295). Explorer: https://hashscan.io/mainnet. Default RPC: https://mainnet.hashio.io/api.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const hedera: Common;

/**
 * `Common` for Hedera Testnet (chain ID 296). Explorer: https://hashscan.io/testnet. Default RPC: https://testnet.hashio.io/api.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const hederaTestnet: Common;

/**
 * `Common` for Hedera Previewnet (chain ID 297). Explorer: https://hashscan.io/previewnet. Default RPC: https://previewnet.hashio.io/api.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const hederaPreviewnet: Common;

/**
 * `Common` for Holesky (chain ID 17000). Explorer: https://holesky.etherscan.io. Default RPC: https://ethereum-holesky-rpc.publicnode.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const holesky: Common;

/**
 * `Common` for Immutable zkEVM (chain ID 13371). Explorer: https://explorer.immutable.com. Default RPC: https://rpc.immutable.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const immutableZkEvm: Common;

/**
 * `Common` for Immutable zkEVM Testnet (chain ID 13473). Explorer: https://explorer.testnet.immutable.com/. Default RPC: https://rpc.testnet.immutable.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const immutableZkEvmTestnet: Common;

/**
 * `Common` for inEVM Mainnet (chain ID 2525). Explorer: https://inevm.calderaexplorer.xyz. Default RPC: https://mainnet.rpc.inevm.com/http.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const inEVM: Common;

/**
 * `Common` for IOTA EVM (chain ID 8822). Explorer: https://explorer.evm.iota.org. Default RPC: https://json-rpc.evm.iotaledger.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const iota: Common;

/**
 * `Common` for IOTA EVM Testnet (chain ID 1075). Explorer: https://explorer.evm.testnet.iotaledger.net. Default RPC: https://json-rpc.evm.testnet.iotaledger.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const iotaTestnet: Common;

/**
 * `Common` for Kakarot Sepolia (chain ID 1802203764). Explorer: https://sepolia.kakarotscan.org. Default RPC: https://sepolia-rpc.kakarot.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const kakarotSepolia: Common;

/**
 * `Common` for Kava EVM (chain ID 2222). Explorer: https://kavascan.com. Default RPC: https://evm.kava.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const kava: Common;

/**
 * `Common` for Kava EVM Testnet (chain ID 2221). Explorer: https://testnet.kavascan.com/. Default RPC: https://evm.testnet.kava.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const kavaTestnet: Common;

/**
 * `Common` for KCC Mainnet (chain ID 321). Explorer: https://explorer.kcc.io. Default RPC: https://kcc-rpc.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const kcc: Common;

/**
 * `Common` for Klaytn (chain ID 8217). Explorer: https://scope.klaytn.com. Default RPC: https://public-en-cypress.klaytn.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const klaytn: Common;

/**
 * `Common` for Klaytn Baobab Testnet (chain ID 1001). Explorer: https://baobab.klaytnscope.com. Default RPC: https://public-en-baobab.klaytn.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const klaytnBaobab: Common;

/**
 * `Common` for Kaia (chain ID 8217). Explorer: https://kaiascan.io. Default RPC: https://public-en.node.kaia.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const kaia: Common;

/**
 * `Common` for Kairos Testnet (chain ID 1001). Explorer: https://kairos.kaiascan.io. Default RPC: https://public-en-kairos.node.kaia.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const kairos: Common;

/**
 * `Common` for Koi Network (chain ID 701). Explorer: https://koi-scan.darwinia.network. Default RPC: https://koi-rpc.darwinia.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const koi: Common;

/**
 * `Common` for Kroma (chain ID 255). Explorer: https://blockscout.kroma.network. Default RPC: https://api.kroma.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const kroma: Common;

/**
 * `Common` for Kroma Sepolia (chain ID 2358). Explorer: https://blockscout.sepolia.kroma.network. Default RPC: https://api.sepolia.kroma.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const kromaSepolia: Common;

/**
 * `Common` for L3X Protocol (chain ID 12324). Explorer: https://explorer.l3x.com. Default RPC: https://rpc-mainnet.l3x.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const l3x: Common;

/**
 * `Common` for L3X Protocol Testnet (chain ID 12325). Explorer: https://explorer-testnet.l3x.com. Default RPC: https://rpc-testnet.l3x.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const l3xTestnet: Common;

/**
 * `Common` for LightLink Pegasus Testnet (chain ID 1891). Explorer: https://pegasus.lightlink.io. Default RPC: https://replicator.pegasus.lightlink.io/rpc/v1.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const lightlinkPegasus: Common;

/**
 * `Common` for LightLink Phoenix Mainnet (chain ID 1890). Explorer: https://phoenix.lightlink.io. Default RPC: https://replicator.phoenix.lightlink.io/rpc/v1.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const lightlinkPhoenix: Common;

/**
 * `Common` for Linea Mainnet (chain ID 59144). Explorer: https://lineascan.build. Default RPC: https://rpc.linea.build.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const linea: Common;

/**
 * `Common` for Linea Goerli Testnet (chain ID 59140). Explorer: https://goerli.lineascan.build. Default RPC: https://rpc.goerli.linea.build.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const lineaGoerli: Common;

/**
 * `Common` for Linea Sepolia Testnet (chain ID 59141). Explorer: https://sepolia.lineascan.build. Default RPC: https://rpc.sepolia.linea.build.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const lineaSepolia: Common;

/**
 * `Common` for Linea Goerli Testnet (chain ID 59140). Explorer: https://goerli.lineascan.build. Default RPC: https://rpc.goerli.linea.build.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const lineaTestnet: Common;

/**
 * `Common` for Lisk (chain ID 1135). Explorer: https://blockscout.lisk.com. Default RPC: https://rpc.api.lisk.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const lisk: Common;

/**
 * `Common` for Lisk Sepolia (chain ID 4202). Explorer: https://sepolia-blockscout.lisk.com. Default RPC: https://rpc.sepolia-api.lisk.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const liskSepolia: Common;

/**
 * `Common` for Localhost (chain ID 1337). Explorer: —. Default RPC: http://127.0.0.1:8545.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const localhost: Common;

/**
 * `Common` for LUKSO (chain ID 42). Explorer: https://explorer.execution.mainnet.lukso.network. Default RPC: https://rpc.mainnet.lukso.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const lukso: Common;

/**
 * `Common` for LUKSO Testnet (chain ID 4201). Explorer: https://explorer.execution.testnet.lukso.network. Default RPC: https://rpc.testnet.lukso.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const luksoTestnet: Common;

/**
 * `Common` for Lycan (chain ID 721). Explorer: https://explorer.lycanchain.com. Default RPC: https://rpc.lycanchain.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const lycan: Common;

/**
 * `Common` for Lyra Chain (chain ID 957). Explorer: https://explorer.lyra.finance. Default RPC: https://rpc.lyra.finance.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const lyra: Common;

/**
 * `Common` for Ethereum (chain ID 1). Explorer: https://etherscan.io. Default RPC: https://eth.merkle.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const mainnet: Common;

/**
 * `Common` for Mandala TC9 (chain ID 595). Explorer: https://blockscout.mandala.aca-staging.network. Default RPC: https://eth-rpc-tc9.aca-staging.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const mandala: Common;

/**
 * `Common` for Manta Pacific Mainnet (chain ID 169). Explorer: https://pacific-explorer.manta.network. Default RPC: https://pacific-rpc.manta.network/http.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const manta: Common;

/**
 * `Common` for Manta Pacific Sepolia Testnet (chain ID 3441006). Explorer: https://pacific-explorer.sepolia-testnet.manta.network. Default RPC: https://pacific-rpc.sepolia-testnet.manta.network/http.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const mantaSepoliaTestnet: Common;

/**
 * `Common` for Manta Pacific Testnet (chain ID 3441005). Explorer: https://pacific-explorer.testnet.manta.network. Default RPC: https://manta-testnet.calderachain.xyz/http.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const mantaTestnet: Common;

/**
 * `Common` for Mantle (chain ID 5000). Explorer: https://mantlescan.xyz/. Default RPC: https://rpc.mantle.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const mantle: Common;

/**
 * `Common` for Mantle Sepolia Testnet (chain ID 5003). Explorer: https://explorer.sepolia.mantle.xyz/. Default RPC: https://rpc.sepolia.mantle.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const mantleSepoliaTestnet: Common;

/**
 * `Common` for Mantle Testnet (chain ID 5001). Explorer: https://explorer.testnet.mantle.xyz. Default RPC: https://rpc.testnet.mantle.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const mantleTestnet: Common;

/**
 * `Common` for Merlin (chain ID 4200). Explorer: https://scan.merlinchain.io. Default RPC: https://rpc.merlinchain.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const merlin: Common;

/**
 * `Common` for MetaChain Mainnet (chain ID 571). Explorer: https://explorer.metatime.com. Default RPC: https://rpc.metatime.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const metachain: Common;

/**
 * `Common` for MetaChain Istanbul (chain ID 1453). Explorer: https://istanbul-explorer.metachain.dev. Default RPC: https://istanbul-rpc.metachain.dev.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const metachainIstanbul: Common;

/**
 * `Common` for Metal L2 (chain ID 1750). Explorer: https://explorer.metall2.com. Default RPC: https://rpc.metall2.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const metalL2: Common;

/**
 * `Common` for Meter (chain ID 82). Explorer: https://scan.meter.io. Default RPC: https://rpc.meter.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const meter: Common;

/**
 * `Common` for Meter Testnet (chain ID 83). Explorer: https://scan-warringstakes.meter.io. Default RPC: https://rpctest.meter.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const meterTestnet: Common;

/**
 * `Common` for Metis (chain ID 1088). Explorer: https://explorer.metis.io. Default RPC: https://metis.rpc.hypersync.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const metis: Common;

/**
 * `Common` for Metis Goerli (chain ID 599). Explorer: https://goerli.explorer.metisdevops.link. Default RPC: https://goerli.gateway.metisdevops.link.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const metisGoerli: Common;

/**
 * `Common` for MEVerse Chain Mainnet (chain ID 7518). Explorer: https://www.meversescan.io. Default RPC: https://rpc.meversemainnet.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const mev: Common;

/**
 * `Common` for MEVerse Chain Testnet (chain ID 4759). Explorer: https://testnet.meversescan.io/. Default RPC: https://rpc.meversetestnet.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const mevTestnet: Common;

/**
 * `Common` for Mint Sepolia Testnet (chain ID 1686). Explorer: https://testnet-explorer.mintchain.io. Default RPC: https://testnet-rpc.mintchain.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const mintSepoliaTestnet: Common;

/**
 * `Common` for Mode Mainnet (chain ID 34443). Explorer: https://modescan.io. Default RPC: https://mainnet.mode.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const mode: Common;

/**
 * `Common` for Mode Testnet (chain ID 919). Explorer: https://sepolia.explorer.mode.network. Default RPC: https://sepolia.mode.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const modeTestnet: Common;

/**
 * `Common` for Moonbase Alpha (chain ID 1287). Explorer: https://moonbase.moonscan.io. Default RPC: https://rpc.api.moonbase.moonbeam.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const moonbaseAlpha: Common;

/**
 * `Common` for Moonbeam (chain ID 1284). Explorer: https://moonscan.io. Default RPC: https://rpc.api.moonbeam.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const moonbeam: Common;

/**
 * `Common` for Moonbeam Development Node (chain ID 1281). Explorer: —. Default RPC: http://127.0.0.1:9944.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const moonbeamDev: Common;

/**
 * `Common` for Moonriver (chain ID 1285). Explorer: https://moonriver.moonscan.io. Default RPC: https://rpc.api.moonriver.moonbeam.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const moonriver: Common;

/**
 * `Common` for Morph Holesky (chain ID 2810). Explorer: https://explorer-holesky.morphl2.io. Default RPC: https://rpc-quicknode-holesky.morphl2.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const morphHolesky: Common;

/**
 * `Common` for Morph Sepolia (chain ID 2710). Explorer: https://explorer-testnet.morphl2.io. Default RPC: https://rpc-testnet.morphl2.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const morphSepolia: Common;

/**
 * `Common` for Nautilus Mainnet (chain ID 22222). Explorer: https://nautscan.com. Default RPC: https://api.nautilus.nautchain.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const nautilus: Common;

/**
 * `Common` for Neon EVM DevNet (chain ID 245022926). Explorer: https://devnet.neonscan.org. Default RPC: https://devnet.neonevm.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const neonDevnet: Common;

/**
 * `Common` for Neon EVM MainNet (chain ID 245022934). Explorer: https://neonscan.org. Default RPC: https://neon-proxy-mainnet.solana.p2p.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const neonMainnet: Common;

/**
 * `Common` for Nexi (chain ID 4242). Explorer: https://www.nexiscan.com. Default RPC: https://rpc.chain.nexi.technology.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const nexi: Common;

/**
 * `Common` for Nexilix Smart Chain (chain ID 240). Explorer: https://scan.nexilix.com. Default RPC: https://rpcurl.pos.nexilix.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const nexilix: Common;

/**
 * `Common` for Oasys (chain ID 248). Explorer: https://scan.oasys.games. Default RPC: https://rpc.mainnet.oasys.games.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const oasys: Common;

/**
 * `Common` for Oasis Testnet (chain ID 4090). Explorer: https://oasis.ftnscan.com. Default RPC: https://rpc1.oasis.bahamutchain.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const oasisTestnet: Common;

/**
 * `Common` for OKC (chain ID 66). Explorer: https://www.oklink.com/okc. Default RPC: https://exchainrpc.okex.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const okc: Common;

/**
 * `Common` for OP Mainnet (chain ID 10). Explorer: https://optimistic.etherscan.io. Default RPC: https://mainnet.optimism.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const optimism: Common;

/**
 * `Common` for Optimism Goerli (chain ID 420). Explorer: https://goerli-optimism.etherscan.io. Default RPC: https://goerli.optimism.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const optimismGoerli: Common;

/**
 * `Common` for OP Sepolia (chain ID 11155420). Explorer: https://optimism-sepolia.blockscout.com. Default RPC: https://sepolia.optimism.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const optimismSepolia: Common;

/**
 * `Common` for opBNB (chain ID 204). Explorer: https://opbnb.bscscan.com. Default RPC: https://opbnb-mainnet-rpc.bnbchain.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const opBNB: Common;

/**
 * `Common` for opBNB Testnet (chain ID 5611). Explorer: https://testnet.opbnbscan.com. Default RPC: https://opbnb-testnet-rpc.bnbchain.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const opBNBTestnet: Common;

/**
 * `Common` for OORT MainnetDev (chain ID 9700). Explorer: https://dev-scan.oortech.com. Default RPC: https://dev-rpc.oortech.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const oortMainnetDev: Common;

/**
 * `Common` for Otim Devnet (chain ID 41144114). Explorer: —. Default RPC: http://devnet.otim.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const otimDevnet: Common;

/**
 * `Common` for Palm (chain ID 11297108109). Explorer: https://palm.chainlens.com. Default RPC: https://palm-mainnet.public.blastapi.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const palm: Common;

/**
 * `Common` for Palm Testnet (chain ID 11297108099). Explorer: https://palm.chainlens.com. Default RPC: https://palm-mainnet.public.blastapi.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const palmTestnet: Common;

/**
 * `Common` for PlayFi Albireo Testnet (chain ID 1612127). Explorer: https://albireo-explorer.playfi.ai. Default RPC: https://albireo-rpc.playfi.ai.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const playfiAlbireo: Common;

/**
 * `Common` for PGN (chain ID 424). Explorer: https://explorer.publicgoods.network. Default RPC: https://rpc.publicgoods.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const pgn: Common;

/**
 * `Common` for PGN (chain ID 58008). Explorer: https://explorer.sepolia.publicgoods.network. Default RPC: https://sepolia.publicgoods.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const pgnTestnet: Common;

/**
 * `Common` for Phoenix Blockchain (chain ID 13381). Explorer: https://phoenixplorer.com. Default RPC: https://rpc.phoenixplorer.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const phoenix: Common;

/**
 * `Common` for Plinga (chain ID 242). Explorer: https://www.plgscan.com. Default RPC: https://rpcurl.mainnet.plgchain.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const plinga: Common;

/**
 * `Common` for Plume Testnet (Legacy) (chain ID 161221135). Explorer: https://testnet-explorer.plumenetwork.xyz. Default RPC: https://testnet-rpc.plumenetwork.xyz/http.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const plumeTestnet: Common;

/**
 * `Common` for Polygon (chain ID 137). Explorer: https://polygonscan.com. Default RPC: https://polygon.drpc.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const polygon: Common;

/**
 * `Common` for Polygon Amoy (chain ID 80002). Explorer: https://amoy.polygonscan.com. Default RPC: https://rpc-amoy.polygon.technology.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const polygonAmoy: Common;

/**
 * `Common` for Polygon Mumbai (chain ID 80001). Explorer: https://mumbai.polygonscan.com. Default RPC: https://80001.rpc.thirdweb.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const polygonMumbai: Common;

/**
 * `Common` for Polygon zkEVM (chain ID 1101). Explorer: https://zkevm.polygonscan.com. Default RPC: https://zkevm-rpc.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const polygonZkEvm: Common;

/**
 * `Common` for Polygon zkEVM Cardona (chain ID 2442). Explorer: https://cardona-zkevm.polygonscan.com. Default RPC: https://rpc.cardona.zkevm-rpc.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const polygonZkEvmCardona: Common;

/**
 * `Common` for Polygon zkEVM Testnet (chain ID 1442). Explorer: https://testnet-zkevm.polygonscan.com. Default RPC: https://rpc.public.zkevm-test.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const polygonZkEvmTestnet: Common;

/**
 * `Common` for PulseChain (chain ID 369). Explorer: https://ipfs.scan.pulsechain.com. Default RPC: https://rpc.pulsechain.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const pulsechain: Common;

/**
 * `Common` for PulseChain V4 (chain ID 943). Explorer: https://scan.v4.testnet.pulsechain.com. Default RPC: https://rpc.v4.testnet.pulsechain.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const pulsechainV4: Common;

/**
 * `Common` for Q Mainnet (chain ID 35441). Explorer: https://explorer.q.org. Default RPC: https://rpc.q.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const qMainnet: Common;

/**
 * `Common` for Q Testnet (chain ID 35443). Explorer: https://explorer.qtestnet.org. Default RPC: https://rpc.qtestnet.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const qTestnet: Common;

/**
 * `Common` for re.al (chain ID 111188). Explorer: https://explorer.re.al. Default RPC: https://rpc.realforreal.gelato.digital.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const real: Common;

/**
 * `Common` for Redbelly Network Testnet (chain ID 153). Explorer: https://redbelly.testnet.routescan.io. Default RPC: https://governors.testnet.redbelly.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const redbellyTestnet: Common;

/**
 * `Common` for Redstone (chain ID 690). Explorer: https://explorer.redstone.xyz. Default RPC: https://rpc.redstonechain.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const redstone: Common;

/**
 * `Common` for Reya Network (chain ID 1729). Explorer: https://explorer.reya.network. Default RPC: https://rpc.reya.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const reyaNetwork: Common;

/**
 * `Common` for Rollux Mainnet (chain ID 570). Explorer: https://explorer.rollux.com. Default RPC: https://rpc.rollux.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const rollux: Common;

/**
 * `Common` for Rollux Testnet (chain ID 57000). Explorer: https://rollux.tanenbaum.io. Default RPC: https://rpc-tanenbaum.rollux.com/.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const rolluxTestnet: Common;

/**
 * `Common` for Ronin (chain ID 2020). Explorer: https://app.roninchain.com. Default RPC: https://api.roninchain.com/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const ronin: Common;

/**
 * `Common` for The Root Network (chain ID 7668). Explorer: https://rootscan.io. Default RPC: https://root.rootnet.live/archive.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const root: Common;

/**
 * `Common` for The Root Network - Porcini (chain ID 7672). Explorer: https://porcini.rootscan.io. Default RPC: https://porcini.rootnet.app/archive.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const rootPorcini: Common;

/**
 * `Common` for Rootstock Mainnet (chain ID 30). Explorer: https://explorer.rsk.co. Default RPC: https://public-node.rsk.co.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const rootstock: Common;

/**
 * `Common` for Rootstock Testnet (chain ID 31). Explorer: https://explorer.testnet.rootstock.io. Default RPC: https://public-node.testnet.rsk.co.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const rootstockTestnet: Common;

/**
 * `Common` for RSS3 VSL Mainnet (chain ID 12553). Explorer: https://scan.rss3.io. Default RPC: https://rpc.rss3.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const rss3: Common;

/**
 * `Common` for RSS3 VSL Sepolia Testnet (chain ID 2331). Explorer: https://scan.testnet.rss3.io. Default RPC: https://rpc.testnet.rss3.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const rss3Sepolia: Common;

/**
 * `Common` for Ronin Saigon Testnet (chain ID 202601). Explorer: https://saigon-explorer.roninchain.com. Default RPC: https://saigon-testnet.roninchain.com/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const saigon: Common;

/**
 * `Common` for Oasis Sapphire (chain ID 23294). Explorer: https://explorer.oasis.io/mainnet/sapphire. Default RPC: https://sapphire.oasis.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const sapphire: Common;

/**
 * `Common` for Oasis Sapphire Testnet (chain ID 23295). Explorer: https://explorer.oasis.io/testnet/sapphire. Default RPC: https://testnet.sapphire.oasis.dev.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const sapphireTestnet: Common;

/**
 * `Common` for SatoshiVM Alpha Mainnet (chain ID 3109). Explorer: https://svmscan.io. Default RPC: https://alpha-rpc-node-http.svmscan.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const satoshiVM: Common;

/**
 * `Common` for SatoshiVM Testnet (chain ID 3110). Explorer: https://testnet.svmscan.io. Default RPC: https://test-rpc-node-http.svmscan.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const satoshiVMTestnet: Common;

/**
 * `Common` for Scroll (chain ID 534352). Explorer: https://scrollscan.com. Default RPC: https://rpc.scroll.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const scroll: Common;

/**
 * `Common` for Scroll Sepolia (chain ID 534351). Explorer: https://sepolia.scrollscan.com. Default RPC: https://sepolia-rpc.scroll.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const scrollSepolia: Common;

/**
 * `Common` for Sei Network (chain ID 1329). Explorer: https://seiscan.io. Default RPC: https://evm-rpc.sei-apis.com/.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const sei: Common;

/**
 * `Common` for Sei Testnet (chain ID 1328). Explorer: https://testnet.seiscan.io. Default RPC: https://evm-rpc-testnet.sei-apis.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const seiTestnet: Common;

/**
 * `Common` for Sepolia (chain ID 11155111). Explorer: https://sepolia.etherscan.io. Default RPC: https://11155111.rpc.thirdweb.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const sepolia: Common;

/**
 * `Common` for Shape Sepolia Testnet (chain ID 11011). Explorer: https://explorer-sepolia.shape.network/. Default RPC: https://sepolia.shape.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const shapeSepolia: Common;

/**
 * `Common` for Shimmer (chain ID 148). Explorer: https://explorer.evm.shimmer.network. Default RPC: https://json-rpc.evm.shimmer.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const shimmer: Common;

/**
 * `Common` for Shimmer Testnet (chain ID 1073). Explorer: https://explorer.evm.testnet.shimmer.network. Default RPC: https://json-rpc.evm.testnet.shimmer.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const shimmerTestnet: Common;

/**
 * `Common` for SKALE | Block Brawlers (chain ID 391845894). Explorer: https://frayed-decent-antares.explorer.mainnet.skalenodes.com. Default RPC: https://mainnet.skalenodes.com/v1/frayed-decent-antares.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleBlockBrawlers: Common;

/**
 * `Common` for SKALE Calypso Hub (chain ID 1564830818). Explorer: https://honorable-steel-rasalhague.explorer.mainnet.skalenodes.com. Default RPC: https://mainnet.skalenodes.com/v1/honorable-steel-rasalhague.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleCalypso: Common;

/**
 * `Common` for SKALE Calypso Testnet (chain ID 974399131). Explorer: https://giant-half-dual-testnet.explorer.testnet.skalenodes.com. Default RPC: https://testnet.skalenodes.com/v1/giant-half-dual-testnet.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleCalypsoTestnet: Common;

/**
 * `Common` for SKALE | CryptoBlades (chain ID 1026062157). Explorer: https://affectionate-immediate-pollux.explorer.mainnet.skalenodes.com. Default RPC: https://mainnet.skalenodes.com/v1/affectionate-immediate-pollux.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleCryptoBlades: Common;

/**
 * `Common` for SKALE | Crypto Colosseum (chain ID 1032942172). Explorer: https://haunting-devoted-deneb.explorer.mainnet.skalenodes.com. Default RPC: https://mainnet.skalenodes.com/v1/haunting-devoted-deneb.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleCryptoColosseum: Common;

/**
 * `Common` for SKALE Europa Hub (chain ID 2046399126). Explorer: https://elated-tan-skat.explorer.mainnet.skalenodes.com. Default RPC: https://mainnet.skalenodes.com/v1/elated-tan-skat.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleEuropa: Common;

/**
 * `Common` for SKALE Europa Testnet (chain ID 1444673419). Explorer: https://juicy-low-small-testnet.explorer.testnet.skalenodes.com. Default RPC: https://testnet.skalenodes.com/v1/juicy-low-small-testnet.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleEuropaTestnet: Common;

/**
 * `Common` for Exorde Network (chain ID 2139927552). Explorer: https://light-vast-diphda.explorer.mainnet.skalenodes.com. Default RPC: https://mainnet.skalenodes.com/v1/light-vast-diphda.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleExorde: Common;

/**
 * `Common` for SKALE | Human Protocol (chain ID 1273227453). Explorer: https://wan-red-ain.explorer.mainnet.skalenodes.com. Default RPC: https://mainnet.skalenodes.com/v1/wan-red-ain.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleHumanProtocol: Common;

/**
 * `Common` for SKALE Nebula Hub (chain ID 1482601649). Explorer: https://green-giddy-denebola.explorer.mainnet.skalenodes.com. Default RPC: https://mainnet.skalenodes.com/v1/green-giddy-denebola.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleNebula: Common;

/**
 * `Common` for SKALE Nebula Testnet (chain ID 37084624). Explorer: https://lanky-ill-funny-testnet.explorer.testnet.skalenodes.com. Default RPC: https://testnet.skalenodes.com/v1/lanky-ill-funny-testnet.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleNebulaTestnet: Common;

/**
 * `Common` for SKALE | Razor Network (chain ID 278611351). Explorer: https://turbulent-unique-scheat.explorer.mainnet.skalenodes.com. Default RPC: https://mainnet.skalenodes.com/v1/turbulent-unique-scheat.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleRazor: Common;

/**
 * `Common` for SKALE Titan Hub (chain ID 1350216234). Explorer: https://parallel-stormy-spica.explorer.mainnet.skalenodes.com. Default RPC: https://mainnet.skalenodes.com/v1/parallel-stormy-spica.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleTitan: Common;

/**
 * `Common` for SKALE Titan Testnet (chain ID 1020352220). Explorer: https://aware-fake-trim-testnet.explorer.testnet.skalenodes.com. Default RPC: https://testnet.skalenodes.com/v1/aware-fake-trim-testnet.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const skaleTitanTestnet: Common;

/**
 * `Common` for Forma Sketchpad (chain ID 984123). Explorer: https://explorer.sketchpad-1.forma.art. Default RPC: https://rpc.sketchpad-1.forma.art.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const sketchpad: Common;

/**
 * `Common` for Soneium Minato Testnet (chain ID 1946). Explorer: https://soneium-minato.blockscout.com. Default RPC: https://rpc.minato.soneium.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const soneiumMinato: Common;

/**
 * `Common` for Songbird Canary-Network (chain ID 19). Explorer: https://songbird-explorer.flare.network. Default RPC: https://songbird-api.flare.network/ext/C/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const songbird: Common;

/**
 * `Common` for Songbird Testnet Coston (chain ID 16). Explorer: https://coston-explorer.flare.network. Default RPC: https://coston-api.flare.network/ext/C/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const songbirdTestnet: Common;

/**
 * `Common` for Sophon Testnet (chain ID 531050104). Explorer: https://explorer.testnet.sophon.xyz. Default RPC: https://rpc.testnet.sophon.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const sophonTestnet: Common;

/**
 * `Common` for Chiliz Spicy Testnet (chain ID 88882). Explorer: http://spicy-explorer.chiliz.com. Default RPC: https://spicy-rpc.chiliz.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const spicy: Common;

/**
 * `Common` for Shardeum Sphinx (chain ID 8082). Explorer: https://explorer-sphinx.shardeum.org. Default RPC: https://sphinx.shardeum.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const shardeumSphinx: Common;

/**
 * `Common` for Shibarium (chain ID 109). Explorer: https://shibariumscan.io. Default RPC: https://rpc.shibrpc.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const shibarium: Common;

/**
 * `Common` for Puppynet Shibarium (chain ID 157). Explorer: https://puppyscan.shib.io. Default RPC: https://puppynet.shibrpc.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const shibariumTestnet: Common;

/**
 * `Common` for Story Testnet (chain ID 1513). Explorer: https://testnet.storyscan.xyz. Default RPC: https://testnet.storyrpc.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const storyTestnet: Common;

/**
 * `Common` for Stratis Mainnet (chain ID 105105). Explorer: https://explorer.stratisevm.com. Default RPC: https://rpc.stratisevm.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const stratis: Common;

/**
 * `Common` for Syscoin Mainnet (chain ID 57). Explorer: https://explorer.syscoin.org. Default RPC: https://rpc.syscoin.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const syscoin: Common;

/**
 * `Common` for Syscoin Tanenbaum Testnet (chain ID 5700). Explorer: https://tanenbaum.io. Default RPC: https://rpc.tanenbaum.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const syscoinTestnet: Common;

/**
 * `Common` for Taraxa Mainnet (chain ID 841). Explorer: https://explorer.mainnet.taraxa.io. Default RPC: https://rpc.mainnet.taraxa.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const taraxa: Common;

/**
 * `Common` for Taiko Mainnet (chain ID 167000). Explorer: https://taikoscan.io. Default RPC: https://rpc.mainnet.taiko.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const taiko: Common;

/**
 * `Common` for Taiko Hekla L2 (chain ID 167009). Explorer: https://hekla.taikoscan.network. Default RPC: https://rpc.hekla.taiko.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const taikoHekla: Common;

/**
 * `Common` for Taiko Jolnir (Alpha-5 Testnet) (chain ID 167007). Explorer: https://explorer.jolnir.taiko.xyz. Default RPC: https://rpc.jolnir.taiko.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const taikoJolnir: Common;

/**
 * `Common` for Taiko Katla (Alpha-6 Testnet) (chain ID 167008). Explorer: https://explorer.katla.taiko.xyz. Default RPC: https://rpc.katla.taiko.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const taikoKatla: Common;

/**
 * `Common` for Taiko (Alpha-3 Testnet) (chain ID 167005). Explorer: https://explorer.test.taiko.xyz. Default RPC: https://rpc.test.taiko.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const taikoTestnetSepolia: Common;

/**
 * `Common` for Taraxa Testnet (chain ID 842). Explorer: https://explorer.testnet.taraxa.io. Default RPC: https://rpc.testnet.taraxa.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const taraxaTestnet: Common;

/**
 * `Common` for Telcoin Adiri Testnet (chain ID 2017). Explorer: https://telscan.io. Default RPC: https://rpc.telcoin.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const telcoinTestnet: Common;

/**
 * `Common` for Telos (chain ID 40). Explorer: https://www.teloscan.io/. Default RPC: https://rpc.telos.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const telos: Common;

/**
 * `Common` for Telos (chain ID 41). Explorer: https://testnet.teloscan.io/. Default RPC: https://rpc.testnet.telos.net.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const telosTestnet: Common;

/**
 * `Common` for Tenet (chain ID 1559). Explorer: https://tenetscan.io. Default RPC: https://rpc.tenet.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const tenet: Common;

/**
 * `Common` for ThaiChain (chain ID 7). Explorer: https://exp.thaichain.org. Default RPC: https://rpc.thaichain.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const thaiChain: Common;

/**
 * `Common` for 5ireChain Thunder Testnet (chain ID 997). Explorer: https://testnet.5irescan.io/. Default RPC: https://rpc.testnet.5ire.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const thunderTestnet: Common;

/**
 * `Common` for Tron (chain ID 728126428). Explorer: https://tronscan.org. Default RPC: https://api.trongrid.io/jsonrpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const tron: Common;

/**
 * `Common` for Unreal (chain ID 18233). Explorer: https://unreal.blockscout.com. Default RPC: https://rpc.unreal-orbit.gelato.digital.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const unreal: Common;

/**
 * `Common` for Vechain (chain ID 100009). Explorer: https://explore.vechain.org. Default RPC: https://mainnet.vechain.org.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const vechain: Common;

/**
 * `Common` for Wanchain (chain ID 888). Explorer: https://wanscan.org. Default RPC: https://gwan-ssl.wandevs.org:56891.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const wanchain: Common;

/**
 * `Common` for Wanchain Testnet (chain ID 999). Explorer: https://wanscan.org. Default RPC: https://gwan-ssl.wandevs.org:46891.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const wanchainTestnet: Common;

/**
 * `Common` for WEMIX (chain ID 1111). Explorer: https://explorer.wemix.com. Default RPC: https://api.wemix.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const wemix: Common;

/**
 * `Common` for WEMIX Testnet (chain ID 1112). Explorer: https://testnet.wemixscan.com. Default RPC: https://api.test.wemix.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const wemixTestnet: Common;

/**
 * `Common` for X1 Testnet (chain ID 1952). Explorer: https://www.oklink.com/xlayer-test. Default RPC: https://xlayertestrpc.okx.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const xLayerTestnet: Common;

/**
 * `Common` for X1 Testnet (chain ID 1952). Explorer: https://www.oklink.com/xlayer-test. Default RPC: https://xlayertestrpc.okx.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const x1Testnet: Common;

/**
 * `Common` for X Layer Mainnet (chain ID 196). Explorer: https://www.oklink.com/xlayer. Default RPC: https://xlayerrpc.okx.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const xLayer: Common;

/**
 * `Common` for Xai Mainnet (chain ID 660279). Explorer: https://explorer.xai-chain.net. Default RPC: https://xai-chain.net/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const xai: Common;

/**
 * `Common` for Xai Testnet (chain ID 37714555429). Explorer: https://testnet-explorer-v2.xai-chain.net. Default RPC: https://testnet-v2.xai-chain.net/rpc.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const xaiTestnet: Common;

/**
 * `Common` for XDC Network (chain ID 50). Explorer: https://xdcscan.com. Default RPC: https://rpc.xdcrpc.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const xdc: Common;

/**
 * `Common` for Apothem Network (chain ID 51). Explorer: https://testnet.xdcscan.com. Default RPC: https://erpc.apothem.network.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const xdcTestnet: Common;

/**
 * `Common` for XR Sepolia (chain ID 2730). Explorer: https://xr-sepolia-testnet.explorer.caldera.xyz. Default RPC: https://xr-sepolia-testnet.rpc.caldera.xyz/http.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const xrSepolia: Common;

/**
 * `Common` for Yooldo Verse (chain ID 50005). Explorer: https://explorer.yooldo-verse.xyz. Default RPC: https://rpc.yooldo-verse.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const yooldoVerse: Common;

/**
 * `Common` for Yooldo Verse Testnet (chain ID 50006). Explorer: https://explorer.testnet.yooldo-verse.xyz. Default RPC: https://rpc.testnet.yooldo-verse.xyz.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const yooldoVerseTestnet: Common;

/**
 * `Common` for ZetaChain (chain ID 7000). Explorer: https://zetascan.com. Default RPC: https://zetachain-evm.blockpi.network/v1/rpc/public.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zetachain: Common;

/**
 * `Common` for ZetaChain Athens Testnet (chain ID 7001). Explorer: https://testnet.zetascan.com. Default RPC: https://zetachain-athens-evm.blockpi.network/v1/rpc/public.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zetachainAthensTestnet: Common;

/**
 * `Common` for Zhejiang (chain ID 1337803). Explorer: https://zhejiang.beaconcha.in. Default RPC: https://rpc.zhejiang.ethpandaops.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zhejiang: Common;

/**
 * `Common` for Zilliqa (chain ID 32769). Explorer: https://evmx.zilliqa.com. Default RPC: https://api.zilliqa.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zilliqa: Common;

/**
 * `Common` for Zilliqa Testnet (chain ID 33101). Explorer: https://evmx.testnet.zilliqa.com. Default RPC: https://dev-api.zilliqa.com.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zilliqaTestnet: Common;

/**
 * `Common` for ZKFair Mainnet (chain ID 42766). Explorer: https://scan.zkfair.io. Default RPC: https://rpc.zkfair.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zkFair: Common;

/**
 * `Common` for ZKFair Testnet (chain ID 43851). Explorer: https://testnet-scan.zkfair.io. Default RPC: https://testnet-rpc.zkfair.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zkFairTestnet: Common;

/**
 * `Common` for zkLink Nova (chain ID 810180). Explorer: https://explorer.zklink.io. Default RPC: https://rpc.zklink.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zkLinkNova: Common;

/**
 * `Common` for zkLink Nova Sepolia Testnet (chain ID 810181). Explorer: https://sepolia.explorer.zklink.io. Default RPC: https://sepolia.rpc.zklink.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zkLinkNovaSepoliaTestnet: Common;

/**
 * `Common` for ZKsync Era (chain ID 324). Explorer: https://explorer.zksync.io/. Default RPC: https://mainnet.era.zksync.io.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zksync: Common;

/**
 * `Common` for ZKsync InMemory Node (chain ID 260). Explorer: —. Default RPC: http://localhost:8011.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zksyncInMemoryNode: Common;

/**
 * `Common` for ZKsync CLI Local Node (chain ID 270). Explorer: —. Default RPC: http://localhost:3050.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zksyncLocalNode: Common;

/**
 * `Common` for ZKsync Sepolia Testnet (chain ID 300). Explorer: https://sepolia.explorer.zksync.io/. Default RPC: https://sepolia.era.zksync.dev.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zksyncSepoliaTestnet: Common;

/**
 * `Common` for Zora (chain ID 7777777). Explorer: https://explorer.zora.energy. Default RPC: https://rpc.zora.energy.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zora: Common;

/**
 * `Common` for Zora Sepolia (chain ID 999999999). Explorer: https://sepolia.explorer.zora.energy/. Default RPC: https://sepolia.rpc.zora.energy.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zoraSepolia: Common;

/**
 * `Common` for Zora Goerli Testnet (chain ID 999). Explorer: https://testnet.explorer.zora.energy. Default RPC: https://testnet.rpc.zora.energy.
 *
 * Usage: pass as `common` to `createMemoryClient`. See `tevmDefault` and other presets in `tevm/common`.
 * @hidden
 * @type {import('../Common.js').Common}
 */
declare const zoraTestnet: Common;

export { type Common, type CommonOptions, type Hardfork, type MockKzg, abstractTestnet, acala, ancient8, ancient8Sepolia, anvil, apexTestnet, arbitrum, arbitrumGoerli, arbitrumNova, arbitrumSepolia, areonNetwork, areonNetworkTestnet, artelaTestnet, assetChainTestnet, astar, astarZkEVM, astarZkyoto, atletaOlympia, aurora, auroraTestnet, auroria, avalanche, avalancheFuji, b3, b3Sepolia, bahamut, base, baseGoerli, baseSepolia, beam, beamTestnet, bearNetworkChainMainnet, bearNetworkChainTestnet, berachainTestnet, berachainTestnetbArtio, bevmMainnet, bitTorrent, bitTorrentTestnet, bitkub, bitkubTestnet, blast, blastSepolia, bob, bobSepolia, boba, bobaSepolia, botanixTestnet, bronos, bronosTestnet, bsc, bscGreenfield, bscTestnet, btr, btrTestnet, bxn, bxnTestnet, canto, celo, celoAlfajores, chiliz, chips, classic, confluxESpace, confluxESpaceTestnet, coreDao, crab, createCommon, createMockKzg, cronos, cronosTestnet, cronoszkEVM, cronoszkEVMTestnet, crossbell, curtis, cyber, cyberTestnet, darwinia, dchain, dchainTestnet, defichainEvm, defichainEvmTestnet, degen, dfk, dodochainTestnet, dogechain, dreyerxMainnet, dreyerxTestnet, edgeless, edgelessTestnet, edgeware, edgewareTestnet, eon, eos, eosTestnet, etherlink, etherlinkTestnet, evmos, evmosTestnet, fantom, fantomSonicTestnet, fantomTestnet, fibo, filecoin, filecoinCalibration, filecoinHyperspace, flare, flareTestnet, flowMainnet, flowPreviewnet, flowTestnet, fluence, fluenceStage, fluenceTestnet, forma, foundry, fraxtal, fraxtalTestnet, funkiMainnet, funkiSepolia, fuse, fuseSparknet, gnosis, gnosisChiado, gobi, goerli, gravity, ham, haqqMainnet, haqqTestedge2, hardhat, harmonyOne, hashkeyTestnet, hedera, hederaPreviewnet, hederaTestnet, holesky, immutableZkEvm, immutableZkEvmTestnet, inEVM, iota, iotaTestnet, iotex, iotexTestnet, jbc, jbcTestnet, kaia, kairos, kakarotSepolia, karura, kava, kavaTestnet, kcc, klaytn, klaytnBaobab, koi, kroma, kromaSepolia, l3x, l3xTestnet, lightlinkPegasus, lightlinkPhoenix, linea, lineaGoerli, lineaSepolia, lineaTestnet, lisk, liskSepolia, localhost, lukso, luksoTestnet, lycan, lyra, mainnet, mandala, manta, mantaSepoliaTestnet, mantaTestnet, mantle, mantleSepoliaTestnet, mantleTestnet, merlin, metachain, metachainIstanbul, metalL2, meter, meterTestnet, metis, metisGoerli, mev, mevTestnet, mintSepoliaTestnet, mode, modeTestnet, moonbaseAlpha, moonbeam, moonbeamDev, moonriver, morphHolesky, morphSepolia, nautilus, neonDevnet, neonMainnet, nexi, nexilix, oasisTestnet, oasys, okc, oortMainnetDev, opBNB, opBNBTestnet, optimism, optimismGoerli, optimismSepolia, otimDevnet, palm, palmTestnet, pgn, pgnTestnet, phoenix, playfiAlbireo, plinga, plumeTestnet, polygon, polygonAmoy, polygonMumbai, polygonZkEvm, polygonZkEvmCardona, polygonZkEvmTestnet, pulsechain, pulsechainV4, qMainnet, qTestnet, real, redbellyTestnet, redstone, reyaNetwork, rollux, rolluxTestnet, ronin, root, rootPorcini, rootstock, rootstockTestnet, rss3, rss3Sepolia, saigon, sapphire, sapphireTestnet, satoshiVM, satoshiVMTestnet, scroll, scrollSepolia, sei, seiTestnet, sepolia, shapeSepolia, shardeumSphinx, shibarium, shibariumTestnet, shimmer, shimmerTestnet, skaleBlockBrawlers, skaleCalypso, skaleCalypsoTestnet, skaleCryptoBlades, skaleCryptoColosseum, skaleEuropa, skaleEuropaTestnet, skaleExorde, skaleHumanProtocol, skaleNebula, skaleNebulaTestnet, skaleRazor, skaleTitan, skaleTitanTestnet, sketchpad, soneiumMinato, songbird, songbirdTestnet, sophonTestnet, spicy, storyTestnet, stratis, syscoin, syscoinTestnet, taiko, taikoHekla, taikoJolnir, taikoKatla, taikoTestnetSepolia, taraxa, taraxaTestnet, telcoinTestnet, telos, telosTestnet, tenet, tevmDefault, thaiChain, thunderTestnet, tron, unreal, vechain, wanchain, wanchainTestnet, wemix, wemixTestnet, x1Testnet, xLayer, xLayerTestnet, xai, xaiTestnet, xdc, xdcTestnet, xrSepolia, yooldoVerse, yooldoVerseTestnet, zetachain, zetachainAthensTestnet, zhejiang, zilliqa, zilliqaTestnet, zkFair, zkFairTestnet, zkLinkNova, zkLinkNovaSepoliaTestnet, zksync, zksyncInMemoryNode, zksyncLocalNode, zksyncSepoliaTestnet, zora, zoraSepolia, zoraTestnet };
