type ConsensusMode = 'noop' | 'light-client';
type LightReadSelector = 'latest' | 'safe' | 'finalized' | bigint;
type LightSyncNetwork = 'mainnet' | 'sepolia' | 'holesky' | 'unknown';
type CheckpointSource = 'explicit' | 'persisted' | 'default' | 'none';
type LightSyncLifecycleStatus = 'idle' | 'starting' | 'syncing' | 'ready' | 'error';
type LightSyncStatus = {
    ready: boolean;
    status: LightSyncLifecycleStatus;
    network: LightSyncNetwork;
    checkpointSource: CheckpointSource;
    lastCheckpoint: string | null;
    optimisticSlot: bigint;
    safeSlot: bigint;
    finalizedSlot: bigint;
};
type ConsensusService = {
    readonly mode: ConsensusMode;
    readonly getLightSyncStatus?: () => Readonly<LightSyncStatus>;
    readonly updateLightSyncStatus?: (next: Partial<LightSyncStatus>) => Readonly<LightSyncStatus>;
    readonly isReady?: () => boolean;
    readonly getChainId?: () => Promise<bigint>;
    readonly getBlockNumber?: () => Promise<bigint>;
    readonly resolveStateRoot?: (selector: LightReadSelector) => Promise<`0x${string}` | undefined>;
    readonly getProof?: (params: {
        readonly address: `0x${string}`;
        readonly storageKeys: readonly `0x${string}`[];
        readonly selector: LightReadSelector;
    }) => Promise<{
        readonly balance: `0x${string}`;
        readonly nonce: `0x${string}`;
        readonly codeHash: `0x${string}`;
        readonly storageHash: `0x${string}`;
        readonly storageProof: readonly {
            readonly key: `0x${string}`;
            readonly value: `0x${string}`;
        }[];
        readonly code?: `0x${string}`;
    }>;
    readonly verifyRead?: (params: {
        readonly blockHash?: `0x${string}`;
        readonly account?: `0x${string}`;
        readonly stateRoot?: `0x${string}`;
        readonly selector?: 'latest' | 'safe' | 'finalized' | `0x${string}`;
        readonly proof?: unknown;
    }) => Promise<boolean>;
};

type LightClientConsensusOptions = {
    readonly verifyRead: NonNullable<ConsensusService['verifyRead']>;
    readonly getChainId?: ConsensusService['getChainId'];
    readonly getBlockNumber?: ConsensusService['getBlockNumber'];
    readonly resolveStateRoot?: ConsensusService['resolveStateRoot'];
    readonly getProof?: ConsensusService['getProof'];
    readonly initialLightSyncStatus?: NonNullable<ConsensusService['getLightSyncStatus']> extends () => infer T ? T : never;
};
declare const createLightClientConsensusService: (options: LightClientConsensusOptions) => ConsensusService;

declare const createNoopConsensusService: () => ConsensusService;

export { type CheckpointSource, type ConsensusMode, type ConsensusService, type LightClientConsensusOptions, type LightReadSelector, type LightSyncLifecycleStatus, type LightSyncNetwork, type LightSyncStatus, createLightClientConsensusService, createNoopConsensusService };
