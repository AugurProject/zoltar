"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Bloom = void 0;
exports.encodeReceipt = encodeReceipt;
exports.encodeReceiptLogs = encodeReceiptLogs;
exports.decodeReceiptLogs = decodeReceiptLogs;
exports.encodeStoredReceipts = encodeStoredReceipts;
exports.decodeStoredReceipts = decodeStoredReceipts;
exports.encodeTxHashIndex = encodeTxHashIndex;
exports.decodeTxHashIndex = decodeTxHashIndex;
const sha3_js_1 = require("@noble/hashes/sha3.js");
const rlp_js_1 = require("./rlp.js");
const BYTE_SIZE = 256;
const LEGACY_TRANSACTION_TYPE = 0;
function concatBytes(parts) {
    if (parts.length === 0)
        return new Uint8Array();
    if (parts.length === 1)
        return parts[0];
    let length = 0;
    for (const part of parts)
        length += part.length;
    const out = new Uint8Array(length);
    let offset = 0;
    for (const part of parts) {
        out.set(part, offset);
        offset += part.length;
    }
    return out;
}
function integerToBytes(value) {
    const bigintValue = typeof value === "bigint" ? value : BigInt(value);
    if (bigintValue < 0n)
        throw new Error("Cannot encode negative integer bytes");
    if (bigintValue === 0n)
        return new Uint8Array();
    let hex = bigintValue.toString(16);
    if (hex.length % 2 === 1)
        hex = `0${hex}`;
    const out = new Uint8Array(hex.length / 2);
    for (let i = 0; i < out.length; i++) {
        out[i] = Number.parseInt(hex.slice(i * 2, i * 2 + 2), 16);
    }
    return out;
}
function bytesToBigInt(bytes) {
    if (bytes.length === 0)
        return 0n;
    let hex = "";
    for (const byte of bytes) {
        hex += byte.toString(16).padStart(2, "0");
    }
    return BigInt(`0x${hex}`);
}
function bytesToNumber(bytes) {
    const value = Number(bytesToBigInt(bytes));
    if (!Number.isSafeInteger(value)) {
        throw new Error("Integer bytes exceed safe number range");
    }
    return value;
}
function asBytes(value, context) {
    if (value instanceof Uint8Array)
        return value;
    throw new Error(`Expected ${context} to be bytes`);
}
function asList(value, context) {
    if (Array.isArray(value))
        return value;
    throw new Error(`Expected ${context} to be an RLP list`);
}
/**
 * Ethereum logs bloom filter.
 */
class Bloom {
    bitvector;
    #keccak;
    constructor(bitvector, common) {
        this.#keccak = common?.customCrypto?.keccak256 ?? sha3_js_1.keccak_256;
        if (bitvector === undefined) {
            this.bitvector = new Uint8Array(BYTE_SIZE);
            return;
        }
        if (bitvector.length !== BYTE_SIZE) {
            throw new Error(`Bloom bitvectors must be ${BYTE_SIZE} bytes long`);
        }
        this.bitvector = bitvector;
    }
    add(value) {
        const hashed = this.#keccak(value);
        const mask = 2047;
        for (let i = 0; i < 3; i++) {
            const first2bytes = new DataView(hashed.buffer, hashed.byteOffset, hashed.byteLength).getUint16(i * 2);
            const loc = mask & first2bytes;
            const byteLoc = loc >> 3;
            const bitLoc = 1 << (loc % 8);
            this.bitvector[BYTE_SIZE - byteLoc - 1] |= bitLoc;
        }
    }
    check(value) {
        const hashed = this.#keccak(value);
        const mask = 2047;
        for (let i = 0; i < 3; i++) {
            const first2bytes = new DataView(hashed.buffer, hashed.byteOffset, hashed.byteLength).getUint16(i * 2);
            const loc = mask & first2bytes;
            const byteLoc = loc >> 3;
            const bitLoc = 1 << (loc % 8);
            if ((this.bitvector[BYTE_SIZE - byteLoc - 1] & bitLoc) === 0) {
                return false;
            }
        }
        return true;
    }
    multiCheck(values) {
        return values.every((value) => this.check(value));
    }
    or(bloom) {
        for (let i = 0; i < BYTE_SIZE; i++) {
            this.bitvector[i] = this.bitvector[i] | bloom.bitvector[i];
        }
    }
}
exports.Bloom = Bloom;
/**
 * Encode an Ethereum transaction receipt, including EIP-2718 typed receipt prefixes.
 */
function encodeReceipt(receipt, txType) {
    const encoded = (0, rlp_js_1.encode)([
        receipt.stateRoot ?? (receipt.status === 0 ? new Uint8Array() : Uint8Array.of(1)),
        integerToBytes(receipt.cumulativeBlockGasUsed),
        receipt.bitvector,
        receipt.logs,
    ]);
    if (txType === LEGACY_TRANSACTION_TYPE) {
        return encoded;
    }
    return concatBytes([integerToBytes(txType), encoded]);
}
function encodeReceiptLogs(logs) {
    return (0, rlp_js_1.encode)(logs);
}
function decodeReceiptLogs(encoded) {
    const decoded = asList((0, rlp_js_1.decode)(encoded), "receipt logs");
    return decoded.map((entry, logIndex) => {
        const log = asList(entry, `receipt log ${logIndex}`);
        const topics = asList(log[1], `receipt log ${logIndex} topics`).map((topic, topicIndex) => asBytes(topic, `receipt log ${logIndex} topic ${topicIndex}`));
        return [
            asBytes(log[0], `receipt log ${logIndex} address`),
            topics,
            asBytes(log[2], `receipt log ${logIndex} data`),
        ];
    });
}
function encodeStoredReceipts(receipts) {
    return (0, rlp_js_1.encode)(receipts.map((receipt) => [
        receipt.stateRoot ?? integerToBytes(receipt.status ?? 0),
        integerToBytes(receipt.cumulativeBlockGasUsed),
        encodeReceiptLogs(receipt.logs),
    ]));
}
function decodeStoredReceipts(encoded) {
    const decoded = asList((0, rlp_js_1.decode)(encoded), "stored receipts");
    return decoded.map((entry, receiptIndex) => {
        const receipt = asList(entry, `stored receipt ${receiptIndex}`);
        const postStateOrStatus = asBytes(receipt[0], `stored receipt ${receiptIndex} status`);
        const cumulativeBlockGasUsed = bytesToBigInt(asBytes(receipt[1], `stored receipt ${receiptIndex} gas used`));
        const logs = decodeReceiptLogs(asBytes(receipt[2], `stored receipt ${receiptIndex} logs`));
        if (postStateOrStatus.length === 32) {
            return {
                stateRoot: postStateOrStatus,
                cumulativeBlockGasUsed,
                logs,
            };
        }
        return {
            status: bytesToNumber(postStateOrStatus),
            cumulativeBlockGasUsed,
            logs,
        };
    });
}
function encodeTxHashIndex([blockHash, txIndex]) {
    return (0, rlp_js_1.encode)([blockHash, integerToBytes(txIndex)]);
}
function decodeTxHashIndex(encoded) {
    const decoded = asList((0, rlp_js_1.decode)(encoded), "transaction hash index");
    return [
        asBytes(decoded[0], "transaction hash index block hash"),
        bytesToNumber(asBytes(decoded[1], "transaction hash index transaction index")),
    ];
}
