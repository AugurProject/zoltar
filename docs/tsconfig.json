{
	"extends": "../tsconfig.json",
	"compilerOptions": {
		"incremental": false,
		"lib": ["ESNext", "DOM", "DOM.Iterable"],
		"moduleDetection": "force"
	},
	"include": ["charts/**/*.ts", "runtime/**/*.ts"]
}
